/** Public browser configuration. Configure manually or with SIGGY_SUPABASE_URL
 * and SIGGY_SUPABASE_ANON_KEY before npm start / npm run build.
 * Never include a service-role, secret, or AI provider key here. */
export const environment = {
  supabaseUrl: "",
  supabasePublishableKey: "",
};
